spring.application.name=calories-detect-backend
# Local development database. Data is kept in memory and recreated on restart.
spring.datasource.url=jdbc:h2:mem:calories_detect
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=validate
